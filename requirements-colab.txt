# Colab T4 / Kaggle P100 — torch is pre-installed by the runtime; install only deltas.
# Run: !pip install -q -r requirements-colab.txt
#
# Pins are lower-bound only so pip can resolve transitive conflicts between
# colpali-engine and MedGemma's transformers requirement. If pip still struggles,
# upgrade transformers first: `pip install -U transformers` then install the rest.

transformers>=4.50.0
accelerate>=1.1.0
peft>=0.13.0
bitsandbytes>=0.44.0
colpali-engine>=0.3.10
open_clip_torch>=2.29.0
faiss-cpu>=1.8.0
pydicom>=2.4.4
pydantic>=2.9.0
pyyaml>=6.0
python-dotenv>=1.0.0
kaggle>=1.6.17
rapidfuzz>=3.10.0
tenacity>=9.0.0
openai>=1.55.0
bert-score>=0.3.13
sacrebleu>=2.4.3
rouge-score>=0.1.2
sentencepiece>=0.2.0
streamlit>=1.40.0
